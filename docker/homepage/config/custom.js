const glow = document.createElement("div");
glow.className = "glow-overlay";
document.body.prepend(glow);

const dots = document.createElement("div");
dots.className = "dots-overlay";
document.body.prepend(dots);
