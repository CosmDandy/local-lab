---
# Без зависимостей: shared/disk подключается из tasks/main.yml явно,
# чтобы гарантировать монтирование data-диска ДО установки/инициализации кластера.
# Docker не требуется — Postgres нативный пакет (PGDG apt-репозиторий).
